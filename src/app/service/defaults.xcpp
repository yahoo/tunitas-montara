// This is -*- c++ -*- in the S.C.O.L.D. stylings that are so popular these days.
#divert <fpp>
namespace app::service::defaults {
  //
  // Default values for use in the std::optional<TYPE>
  //
  auto const EMIT_PID = false;
  auto const FOREGROUND_MODE = true;
  auto const SERVICE_PORT = 23118; // [[FIXTIHS]] tie this to a --enable-service-port=NUMBER option in configure.ac
}
#endiv
